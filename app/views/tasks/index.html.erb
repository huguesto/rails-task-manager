<p>ceci est l'index de mes tasks</p>
<!-- link_to show view -->
<!-- link to edit view -->
<!-- link to delete action *** -->

<% @tasks.each do |task| %>
  <h2><%= task.name %></h2>
  <p><%= task.description %></p>
  <p><%= task.done %></p>
  <p> <%= link_to "Delete", task_path(task), method: :delete, data: { confirm: "Are you sure?" }%></p>
  <p> <%= link_to "Edit",edit_task_path(task) %></p>
  <p> <%= link_to "Show", task_path(task) %></p>
<% end %>

<%= link_to "Create a new task", "tasks/new" %>


